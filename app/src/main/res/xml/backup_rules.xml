<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Progress is worth restoring; the entitlement cache is not (Play is the source of truth). -->
    <include domain="database" path="pushup.db" />
    <include domain="sharedpref" path="settings.xml" />
    <exclude domain="sharedpref" path="billing_cache.xml" />
</full-backup-content>
