# 결정 사항

2026-09-14에 확정. 여기 없는 것은 아직 결정되지 않았거나 코드가 기본값을 고른 것입니다.

## 범위

| | |
|---|---|
| v1 운동 | 푸쉬업 + 스쿼트 + 플랭크 |
| 캐릭터 아트 | 도형 기반으로 직접 구현 (Compose Canvas). 외주 아트는 v2 |
| 오디오 | 최소 SFX — 인정 / 깊게 / 피격 / 콤보 / 필살기 예고 |
| 첫 출시 트랙 | 비공개 테스트 (개인 개발자 계정은 프로덕션 전에 테스터 12명 × 14일 필요) |

## 비즈니스 모델

| | |
|---|---|
| 무료 범위 | 첫 던전 + 고냥이 모드 영구 무료. 운동 자체는 절대 잠그지 않음 |
| 구독 | 월간 + 연간. 페이월은 연간 기본 선택, 월 환산액 병기 |
| 무료 체험 | 7일 |
| 광고 | 없음 |

무료 범위는 `FreeTier` 한 곳에서만 결정됩니다. 다른 곳에 판정 로직을 두지 마세요 —
이미 한 번, 홈 화면의 가장 큰 버튼이 그 검사를 건너뛰고 있었습니다.

## 기술·운영

| | |
|---|---|
| 앱 ID | `io.github.cjsdudwls1.pushuprpg` |
| 크래시·분석 | Firebase Crashlytics + Analytics |
| 고냥이 모드 위치 | 온보딩 튜토리얼. 천장을 밀어올리는 동작이 말 없이 깊이를 가르칩니다 |

앱 ID는 프로덕션 출시 후 영원히 바꿀 수 없습니다. 코드 패키지(`namespace`)는
`com.pushuprpg.app` 그대로 두었습니다 — 둘은 원래 별개이고, 37개 파일을 일괄 리네임하는 것은
이 환경에서 컴파일로 검증할 수 없는 종류의 변경이라 위험 대비 이득이 없습니다. 원하면
Android Studio의 리팩터링으로 언제든 맞출 수 있습니다.

Crashlytics를 넣기로 한 이상 개인정보처리방침과 Play Data Safety 신고에 반영해야 합니다.
"영상은 어디로도 전송되지 않는다"는 여전히 사실이고 그대로 유지되지만, 크래시 로그와 사용
이벤트는 전송되므로 그 둘을 분리해서 써야 합니다.

## 운동 종류는 던전 입장 때 고른다 — 자동 인식은 만들었다가 버렸다

한 판에는 한 가지 운동. 고르는 곳은 **던전 입장 화면**이고, 설정에는 없습니다.

카메라로 종목을 알아내는 버전을 만들어 봤고, **동작을 맞히기는 했는데 그래도 틀렸습니다.**
검출기 아홉 개를 동시에 돌려서 먼저 인정한 놈을 종목으로 삼는 구조였는데, 진 놈의 타격은
버려집니다. 그래서 승부가 갈리기 전에 한 개수는 그냥 사라졌습니다. 실제 `BattleEngine`으로
측정한 값:

| 종목 바꾸는 사이 쉰 시간 | 인정된 풀업 | 몬스터 데미지 | 내 HP |
|---|---|---|---|
| 0초 | 0 / 6 | 0 | −39 |
| 2초 | 1 / 6 | 26 | −27 |
| 10초 | 3 / 6 | 81 | −7 |
| 20초+ | 6 / 6 | — | 0 |

바닥에서 철봉으로 바로 가면 여섯 개가 전부 없어집니다. 풀업은 교차검증 증인이 하나뿐이라
(매달린 몸은 머리가 어깨 기준으로 움직이지 않아서 푸쉬업이 쓰는 witness를 쓸 수 없습니다)
개수로는 푸쉬업을 절대 못 이기고, 20초 증거 창이 만료되기만 기다립니다. 빠른 템포로 하면
12개 중 9개가 사라집니다 — **빨리 할수록 더 많이 잃습니다.**

덤으로 두 가지가 더 나왔습니다. 가만히 20초 있으면 세션이 PLANK로 바뀌고 `onHold` 데미지가
나가서, **40초 안 움직이니 던전 2층이 깨졌습니다(반복 0개)**. 그리고 푸쉬업만 해도 풀업
검출기가 뒤에서 같이 세기 때문에 저장되는 풀업 캘리브레이션이 푸쉬업 것과 숫자까지
같아집니다.

교훈은 "라우터를 더 잘 만들자"가 아닙니다. 푸쉬업 탑에서 쉬는 자세와 플랭크는 카메라가 볼 수
있는 한 **같은 자세**입니다. 구분할 수 없는 것을 추측하게 두면 안 됩니다.

그때 통과하던 슈퍼세트 테스트는 2초 간격으로 마지막 이름표만 확인했기 때문에, 풀업 6개 중
5개가 버려지는 동안 초록불이었습니다. **마지막 상태만 보는 테스트는 통과해도 아무것도
보장하지 않습니다.**

남긴 것: `RepDetectorImpl.strikeBlockedReason`이 선언된 교차검증을 하나만 골라서 쓰던 버그의
수정. 라우터와 무관한 검출기 자체의 결함이었고(머리를 고정하면 푸쉬업을 속일 수 있었습니다),
`PullUpDetectorTest.the pushup detector refuses a pull-up`이 그것을 고정합니다.

## 아직 결정되지 않은 것

- 구독 실제 가격 (Play 콘솔에서 설정. 코드는 `ProductDetails`에서 읽기만 합니다)
- 개인정보처리방침 호스팅 위치
- 건강 면책 문구
- 스토어 등재 정보와 스크린샷
